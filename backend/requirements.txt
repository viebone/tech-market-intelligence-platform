fastapi==0.115.0
uvicorn[standard]==0.30.6
google-genai>=1.0.0
python-dotenv==1.0.1
httpx>=0.27.0
psycopg[binary]>=3.2.0

# Pipeline visibility admin dashboard (admin_main.py) — see backend/specs/
# pipeline-visibility/api.md.
jinja2>=3.1.0
python-multipart>=0.0.9
pyjwt>=2.9.0
# Direct bcrypt, not passlib[bcrypt] — passlib is unmaintained (last release
# 2020) and its backend self-test breaks against bcrypt>=4.0 (AttributeError:
# module 'bcrypt' has no attribute '__about__'), confirmed the hard way while
# verifying this implementation against real dependencies, 2026-08-16.
bcrypt>=4.0.0
